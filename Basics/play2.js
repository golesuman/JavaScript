let name = 'suman';
let age = 20;
const hasHobbies = true;

function summerizeUser(userName, userAge, userHasHobby)
{
    return ("name is " + userName + 
    "age " + userAge + "and has " + hasHobbies);
}

console.log(summerizeUser(name, age, hasHobbies))